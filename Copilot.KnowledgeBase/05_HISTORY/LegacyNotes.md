# Notas de Código Legado

- O serviço `OpenAIService` referencia tipos de SDK ainda não incluído no projeto (ex.: `OpenAIClient`). É necessário definir/instalar o pacote oficial pretendido e ajustar as chamadas.
- Endpoints atuais são mínimos; módulos listados (Auth, Finance, Storage, Notifications) ainda serão implementados.
